describe("suite2", () => {
  test("test1", () => {
    expect(1 + 1).toBe(3);
  });

  test("test2", () => {
    expect(1 + 1).toBe(2);
  });

  test("test3", () => {
    expect(1 + 1).toBe(0);
  });
});
